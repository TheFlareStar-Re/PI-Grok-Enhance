# Grok Enhance

PI-Desktop 插件。给 Grok 加执行纪律，并在每轮第一请求直接激活 Grep / Glob（以及已装的 memory / skill_manage）。

## 安装

导入 `dist` 里 **0.1.1** 的 `.piplug`。导入后彻底退出 PI（含托盘）再开。

## 打包

```bash
python scripts/pack-piplug.py
```
